version: "3.1"
networks:
  jenkins:
services:
  jenkins-moka:
    build: jenkins
    networks:
      - jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - ./data:/var/jenkins_home

  jenkins-nginx:
    build: nginx
    depends_on:
      - jenkins-moka
    networks:
      - jenkins
    ports:
      - "3001:3001"
